html

head
    title Program
    meta(charset="UTF-8")
    style(type="text/css").
        ul{
            list-style-type: none;
        }
        a{
            text-decoration: none;
        }
        a:visited{
            color: blue;
        }

body
    h1 List of users
    a(href="/newUser") Create new user
    if users.length > 0
        each user in users
            ul
                li= user.username + " - " + user.email
                a(href='/deleteUser/' + user.id) Delete  

                a(href='/editUser/' + user.id) Edit  

                a(href='/userDetail/' + user.id) View More Details
    else
        p Not seeing a list of users? 
        a(href="/initDB") Populate it. 
        p (With some default data.)